cat > js/main.js << 'EOF'
document.addEventListener('DOMContentLoaded',()=>{document.getElementById('year').textContent=new Date().getFullYear();const uploadZone=document.getElementById('upload-zone');const fileInput=document.getElementById('file-input');const uploadPreview=document.getElementById('upload-preview');const searchBtn=document.getElementById('search-btn');uploadZone.addEventListener('click',()=>fileInput.click());fileInput.addEventListener('change',e=>{if(e.target.files[0]){const reader=new FileReader();reader.onload=ev=>{uploadPreview.innerHTML=`<img src="${ev.target.result}" alt="Preview">`;searchBtn.disabled=false;searchBtn.textContent='Искать похожие модели';};reader.readAsDataURL(e.target.files[0]);}});['dragenter','dragover'].forEach(evt=>uploadZone.addEventListener(evt,e=>{e.preventDefault();uploadZone.style.borderColor='var(--accent)';}));['dragleave','drop'].forEach(evt=>uploadZone.addEventListener(evt,e=>{e.preventDefault();uploadZone.style.borderColor='var(--border)';}));uploadZone.addEventListener('drop',e=>{fileInput.files=e.dataTransfer.files;fileInput.dispatchEvent(new Event('change'));});searchBtn.addEventListener('click',()=>{if(searchBtn.disabled)return;document.getElementById('search-results').innerHTML='<p style="color:var(--text-muted);margin-top:20px;">🔍 Анализ изображения...</p>';});document.getElementById('cart-btn').addEventListener('click',()=>alert('🛒 Корзина'));});
EOF
